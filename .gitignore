# --- Dependencies ---
node_modules/
package-lock.json
# (Giữ package-lock.json nếu bạn muốn đồng bộ version chính xác, nhưng thường bỏ qua nếu team nhỏ)

# --- Environment Variables (BẢO MẬT: CỰC KỲ QUAN TRỌNG) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log

# --- Firebase ---
.firebase/
firebase-debug.log

# --- IDE / System Files ---
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# --- Uploads (Lưu ảnh local) ---
# Chúng ta ignore các file ảnh để không nặng git
public/uploads/*

# NHƯNG giữ lại file .gitkeep để git vẫn tạo thư mục uploads khi clone về
!public/uploads/.gitkeep

# --- Firebase Service Account Key ---
src/config/serviceAccountKey.json
